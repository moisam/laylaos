diff -rub ./shadow-4.19.2/configure ./shadow-4.19.2/configure
--- ./shadow-4.19.2/configure	2026-01-19 14:50:06.000000000 +0000
+++ ./shadow-4.19.2/configure	2026-01-20 21:07:27.816282054 +0000
@@ -7647,6 +7647,10 @@
   lt_cv_deplibs_check_method=pass_all
   ;;
 
+laylaos*)
+  lt_cv_deplibs_check_method=pass_all
+  ;;
+
 # This must be glibc/ELF.
 linux* | k*bsd*-gnu | kopensolaris*-gnu | gnu*)
   lt_cv_deplibs_check_method=pass_all
@@ -13123,6 +13127,19 @@
   hardcode_into_libs=yes
   ;;
 
+# LaylaOS
+laylaos*)
+  version_type=linux # correct to gnu/linux during the next big refactor
+  need_lib_prefix=no
+  need_version=no
+  library_names_spec='$libname$release$shared_ext$versuffix $libname$release$shared_ext$major $libname$shared_ext'
+  soname_spec='$libname$release$shared_ext$major'
+  shlibpath_var=LD_LIBRARY_PATH
+  shlibpath_overrides_runpath=no
+  dynamic_linker='LaylaOS ld.so'
+  hardcode_into_libs=yes
+  ;;
+
 # No shared lib support for Linux oldld, aout, or coff.
 linux*oldld* | linux*aout* | linux*coff*)
   dynamic_linker=no
