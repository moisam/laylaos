diff -rub ./grep-3.11/src/grep.c ./grep-3.11/src/grep.c
--- ./grep-3.11/src/grep.c	2023-04-11 01:20:47.000000000 +0100
+++ ./grep-3.11/src/grep.c	2024-11-17 09:14:06.268810630 +0000
@@ -1774,7 +1774,7 @@
   ssize_t nbytes;
   if (S_ISFIFO (st->st_mode) && dev_null_output)
     {
-#ifdef SPLICE_F_MOVE
+#if defined(SPLICE_F_MOVE) && !defined(__laylaos__)
       /* Should be faster, since it need not copy data to user space.  */
       nbytes = splice (fd, NULL, STDOUT_FILENO, NULL,
                        INITIAL_BUFSIZE, SPLICE_F_MOVE);
