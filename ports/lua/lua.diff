diff -rub ./lua-5.2.4/Makefile ./lua-5.2.4/Makefile
--- ./lua-5.2.4/Makefile	2015-02-24 01:28:54.000000000 +0000
+++ ./lua-5.2.4/Makefile	2024-08-14 21:55:03.813927777 +0100
@@ -36,12 +36,12 @@
 # == END OF USER SETTINGS -- NO NEED TO CHANGE ANYTHING BELOW THIS LINE =======
 
 # Convenience platforms targets.
-PLATS= aix ansi bsd freebsd generic linux macosx mingw posix solaris
+PLATS= aix ansi bsd freebsd generic laylaos linux macosx mingw posix solaris
 
 # What to install.
 TO_BIN= lua luac
 TO_INC= lua.h luaconf.h lualib.h lauxlib.h lua.hpp
-TO_LIB= liblua.a
+TO_LIB= liblua.a liblua.so
 TO_MAN= lua.1 luac.1
 
 # Lua version and release.
diff -rub ./lua-5.2.4/src/luaconf.h ./lua-5.2.4/src/luaconf.h
--- ./lua-5.2.4/src/luaconf.h	2013-11-21 17:26:16.000000000 +0000
+++ ./lua-5.2.4/src/luaconf.h	2024-08-14 21:59:06.095862302 +0100
@@ -59,6 +59,15 @@
 #endif
 
 
+#if defined(LUA_USE_LAYLAOS)
+#ifndef LUA_USE_POSIX
+#define LUA_USE_POSIX
+#endif
+#define LUA_USE_MKSTEMP
+#define LUA_USE_ISATTY
+#define LUA_USE_DLOPEN
+#endif
+
 
 /*
 @@ LUA_USE_POSIX includes all functionality listed as X/Open System
@@ -100,7 +109,13 @@
 #else			/* }{ */
 
 #define LUA_VDIR	LUA_VERSION_MAJOR "." LUA_VERSION_MINOR "/"
+
+#if defined(LUA_USE_LAYLAOS)
+#define LUA_ROOT	"/usr/"
+#else
 #define LUA_ROOT	"/usr/local/"
+#endif
+
 #define LUA_LDIR	LUA_ROOT "share/lua/" LUA_VDIR
 #define LUA_CDIR	LUA_ROOT "lib/lua/" LUA_VDIR
 #define LUA_PATH_DEFAULT  \
diff -rub ./lua-5.2.4/src/Makefile ./lua-5.2.4/src/Makefile
--- ./lua-5.2.4/src/Makefile	2013-11-11 11:45:49.000000000 +0000
+++ ./lua-5.2.4/src/Makefile	2024-08-15 18:39:35.996506055 +0100
@@ -6,13 +6,13 @@
 # Your platform. See PLATS for possible values.
 PLAT= none
 
-CC= gcc
-CFLAGS= -O2 -Wall -DLUA_COMPAT_ALL $(SYSCFLAGS) $(MYCFLAGS)
+CC?= gcc
+CFLAGS= -O2 -Wall -fPIC -mstackrealign -DLUA_COMPAT_ALL $(SYSCFLAGS) $(MYCFLAGS)
 LDFLAGS= $(SYSLDFLAGS) $(MYLDFLAGS)
 LIBS= -lm $(SYSLIBS) $(MYLIBS)
 
-AR= ar rcu
-RANLIB= ranlib
+AR?= ar rcu
+RANLIB?= ranlib
 RM= rm -f
 
 SYSCFLAGS=
@@ -26,9 +26,10 @@
 
 # == END OF USER SETTINGS -- NO NEED TO CHANGE ANYTHING BELOW THIS LINE =======
 
-PLATS= aix ansi bsd freebsd generic linux macosx mingw posix solaris
+PLATS= aix ansi bsd freebsd generic laylaos linux macosx mingw posix solaris
 
 LUA_A=	liblua.a
+LUA_SO= liblua.so
 CORE_O=	lapi.o lcode.o lctype.o ldebug.o ldo.o ldump.o lfunc.o lgc.o llex.o \
 	lmem.o lobject.o lopcodes.o lparser.o lstate.o lstring.o ltable.o \
 	ltm.o lundump.o lvm.o lzio.o
@@ -43,7 +44,7 @@
 LUAC_O=	luac.o
 
 ALL_O= $(BASE_O) $(LUA_O) $(LUAC_O)
-ALL_T= $(LUA_A) $(LUA_T) $(LUAC_T)
+ALL_T= $(LUA_A) $(LUA_T) $(LUAC_T) $(LUA_SO)
 ALL_A= $(LUA_A)
 
 # Targets start here.
@@ -65,8 +66,11 @@
 $(LUAC_T): $(LUAC_O) $(LUA_A)
 	$(CC) -o $@ $(LDFLAGS) $(LUAC_O) $(LUA_A) $(LIBS)
 
+$(LUA_SO): $(CORE_O) $(LIB_O)
+	$(CC) -o $@ -shared -fPIC -Wl,-soname=liblua.so $(LDFLAGS) $? $(LIBS)
+
 clean:
-	$(RM) $(ALL_T) $(ALL_O)
+	$(RM) $(ALL_T) $(ALL_O) $(ALL_SO)
 
 depend:
 	@$(CC) $(CFLAGS) -MM l*.c
@@ -102,6 +106,9 @@
 
 generic: $(ALL)
 
+laylaos:
+	$(MAKE) $(ALL) SYSCFLAGS="-DLUA_USE_LAYLAOS -DLUA_USE_POSIX" LIBS=
+
 linux:
 	$(MAKE) $(ALL) SYSCFLAGS="-DLUA_USE_LINUX" SYSLIBS="-Wl,-E -ldl -lreadline"
 
